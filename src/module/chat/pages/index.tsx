import ChatWrapper from "../components/ChatWrapper";

export default function ChatPage() {
  return (
    <div className="h-full min-h-0 overflow-hidden rounded-2xl border bg-card shadow-[var(--shadow-panel)]">
      <ChatWrapper />
    </div>
  );
}
